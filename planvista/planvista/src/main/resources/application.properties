# application name
spring.application.name=planvista

# DB configure
spring.datasource.url=jdbc:mysql://localhost:3306/curriculum?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=curriculum_user
spring.datasource.password=curriculum_password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Flyway configure
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Thymeleaf configure
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/

# Server configure
server.servlet.session.timeout=300m

# Log configure
logging.level.org.springframework.security=DEBUG
logging.level.com.example.planvista=DEBUG

spring.output.ansi.enabled=ALWAYS

# DevTools configure
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# MVC configure
spring.mvc.hiddenmethod.filter.enabled=true

# Spring Security configure
#logging.level.org.springframework.security.authentication=TRACE

# Server設定
server.port=8080

# Google OAuth2設定
spring.security.oauth2.client.registration.google.client-id=340383820494-7rr40go8b4jsf1lr43n9c0b0nbetptip.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=https://www.googleapis.com/auth/calendar.readonly,https://www.googleapis.com/auth/calendar.events,openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}